
<%= render 'shared/banner', background_image_url: image_path('standup.jpg') %>


   <% @list.movies.each do |movie|%>
    <div class="card-product">
      <%= image_tag(movie.poster_url, alt: "movie picture", class: "card-product img") %>
    <div class="card-product-infos">
      <h2><%=  movie.title  %></h2>
      <p><%= movie.overview  %></p>
    </div>
    </div>
    <% end %>
<%# <%= lintk_to "add a movie ", new_list_bookmark_url, class: "btn btn-gradient" %> %>
 <%= link_to "delete this list", list_path(@list), class: "btn btn-cancel", data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this list?" } %>
